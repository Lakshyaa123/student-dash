import React from "react";
import "./Navbar.css";

const Navbar = () => {
  return (
    <nav className="navbar">
      <a href="#" className="nav-item">Dashboard</a>
      <a href="#" className="nav-item">Placements</a>
      <a href="#" className="nav-item">Settings</a>
    </nav>
  );
};

export default Navbar;
